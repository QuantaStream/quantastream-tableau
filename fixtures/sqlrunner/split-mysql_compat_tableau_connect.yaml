# Generated by scripts/trace_to_sqlrunner.py.
# Review IDs, features, and expected results before moving this into the QuantaStream engine repo.
version: 1
name: mysql_compat_tableau_connect

tests:
  - id: mysql_compat_tableau_connect.001.set_names_utf8mb4_b972be11
    status: supported
    kind: statement
    feature: tableau_connect_capture
    compatibility: mysql
    requires: [tableau_capture, tableau_connect]
    sql: |
      set names utf8mb4

  - id: mysql_compat_tableau_connect.002.version_version_value_version_comment_version_comment_2b1c12e6
    status: supported
    kind: query
    feature: tableau_connect_capture
    compatibility: mysql
    requires: [tableau_capture, tableau_connect]
    sql: |
      select @@version as version_value, @@version_comment as version_comment

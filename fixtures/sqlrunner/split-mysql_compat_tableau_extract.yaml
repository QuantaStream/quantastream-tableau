# Generated by scripts/trace_to_sqlrunner.py.
# Review IDs, features, and expected results before moving this into the QuantaStream engine repo.
version: 1
name: mysql_compat_tableau_extract

tests:
  - id: mysql_compat_tableau_extract.001.count_row_count_order_id_superstore_orders_6fc1e65d
    status: supported
    kind: query
    feature: tableau_extract_capture
    compatibility: mysql
    requires: [tableau_capture, tableau_extract]
    sql: |
      select count(*) as row_count from ( select order_id from superstore_orders ) TableauExtract

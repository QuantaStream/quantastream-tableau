# Generated by scripts/trace_to_sqlrunner.py.
# Review IDs, features, and expected results before moving this into the QuantaStream engine repo.
version: 1
name: mysql_compat_tableau_capture

tests:
  - id: mysql_compat_tableau_capture.001.set_names_utf8mb4_b972be11
    status: supported
    kind: statement
    feature: tableau_connect_capture
    compatibility: mysql
    requires: [tableau_capture, tableau_connect]
    sql: |
      set names utf8mb4

  - id: mysql_compat_tableau_capture.002.version_version_value_version_comment_version_comment_2b1c12e6
    status: supported
    kind: query
    feature: tableau_connect_capture
    compatibility: mysql
    requires: [tableau_capture, tableau_connect]
    sql: |
      select @@version as version_value, @@version_comment as version_comment

  - id: mysql_compat_tableau_capture.003.show_full_tables_7ba7c97e
    status: supported
    kind: query
    feature: tableau_metadata_capture
    compatibility: mysql
    requires: [tableau_capture, tableau_metadata]
    sql: |
      show full tables

  - id: mysql_compat_tableau_capture.004.region_sum_sales_sales_65d19795
    status: supported
    kind: query
    feature: tableau_worksheet_capture
    compatibility: mysql
    requires: [tableau_capture, tableau_worksheet]
    sql: |
      select region, sum(sales) as sales from superstore_orders group by region order by sales desc limit 10

  - id: mysql_compat_tableau_capture.005.region_category_sales_superstore_orders_942096a5
    status: supported
    kind: query
    feature: tableau_custom_sql_capture
    compatibility: mysql
    requires: [tableau_capture, tableau_custom_sql]
    sql: |
      select * from ( select region, category, sales from superstore_orders ) TableauSQL where TableauSQL.sales > 0 limit 10

  - id: mysql_compat_tableau_capture.006.count_row_count_order_id_superstore_orders_6fc1e65d
    status: supported
    kind: query
    feature: tableau_extract_capture
    compatibility: mysql
    requires: [tableau_capture, tableau_extract]
    sql: |
      select count(*) as row_count from ( select order_id from superstore_orders ) TableauExtract

  - id: mysql_compat_tableau_capture.007.unsupported_over_cb420c40
    status: xfail
    kind: query
    feature: tableau_worksheet_capture
    compatibility: mysql
    requires: [tableau_capture, tableau_worksheet]
    sql: |
      select unsupported_over()
    expect:
      error: "parser_boundary"
